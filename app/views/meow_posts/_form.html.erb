<%= form_for(meow_post) do |f| %>
  <% if meow_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meow_post.errors.count, "error") %> prohibited this meow_post from being saved:</h2>

      <ul>
      <% meow_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_field :content %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
